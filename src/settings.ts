interface ISettings {
  mainTitle: string
  fixedHeader: boolean
  showSidebarLogo: boolean
}

const settings: ISettings = {
  mainTitle: 'i admin',
  fixedHeader: false,
  showSidebarLogo: false
}

export default settings
